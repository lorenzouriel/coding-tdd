[project]
name = "pytest-tdd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest==7.1.2",
    "pytest-cov==3.0.0",
]
